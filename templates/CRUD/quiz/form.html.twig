{% extends 'layouts/base.html.twig' %}

{% block meta_title %}Quizzler? - Quizzes{% endblock %}
{% block title %}
    {% if (quiz.id) %}Edit "{{ quiz.value }}"
    {% else %}Create quiz{% endif %}
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
